/*
SCRAPY DEPENDENCIES
step 1 : pip install pipenv
step 2 : virtualenv .       #creating a virtual environment
step 3 : for windows-> .\Scripts\activate            #activating virtual environment
         for MAC/Linux-> source* ./bin/activate
step 4 : pip install scrapy   #installing scrapy
step 5 : work with scrapy -> e.g. scrapy startproject <projectName>
*/

/*
TAG GENERATION DEPENDENCIES(FLAIR)
Requirements and Installation
The project is based on PyTorch 0.4+ and Python 3.6+, Then, in your favorite virtual environment, simply do:

pip install flair
*/
